{
	"name": "@ouroboros/manage",
	"version": "1.0.1",
	"description": "Package to handle the Manage service.",
	"main": "index.js",
	"types": "index.d.ts",
	"scripts": {
		"build": "tslint -p tsconfig.json && tsc",
		"clean": "rm build/*",
		"for_local": "npm install; tslint -p tsconfig.json && tsc; rm -Rf node_modules",
		"test": "echo \"Error: no test specified.\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ouroboroscoding/body-manage-js.git"
	},
	"keywords": [],
	"author": "Chris Nasr <chris@ouroboroscoding.com>",
	"license": "CUSTOM",
	"bugs": {
		"url": "https://github.com/ouroboroscoding/body-manage-js/issues"
	},
	"devDependencies": {
		"tslint": "^6.1.3",
		"tslint-config-prettier": "^1.18.0",
		"typescript": "^5.9.3"
	},
	"peerDependencies": {
		"@ouroboros/body": "^1.3.0"
	}
}